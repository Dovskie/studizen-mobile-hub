
import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.lovable.e6a750ad7fee4447957cbebbfbebcb54',
  appName: 'studizen-mobile-hub',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
